# Caddy reverse-proxy – environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env && $EDITOR .env
#
# NEVER commit .env to version control – it is listed in .gitignore.
#
# Caddy substitutes {$VAR} and {$VAR:default} placeholders in the Caddyfile
# at startup using these environment variables. No secrets are hard-coded in
# the Caddyfile.
#
#
# ============================================================================
# Basic-auth credentials
# ============================================================================
# Username for the protected routes
PROXY_AUTH_USER=admin

# Bcrypt hash of the password (single $ signs).
# Generate with:  make gen-hash
# Then paste the "Caddy format" line (single $) here.
#
# To load from an external secrets manager instead of storing here, leave this
# blank and export the variable before running make:
#   export PROXY_AUTH_HASH="$(vault kv get -field=hash secret/proxy-auth)"
#   make up
# NOTE:
#  - To pass a dollar sign in a .env file, you must specify it twice
#    (ex. instead of `some$var`, use `some$$var`)
PROXY_AUTH_HASH=

# ============================================================================
# TLS / domain settings
# ============================================================================
# Base domain used in the Caddyfile site block.
# Examples: localhost | thinkpaddy.local
PROXY_DOMAIN=localhost

OLLAMA_HOST=ollama
OLLAMA_PORT=11434

LLAMASWAP_HOST=host.docker.internal
LLAMASWAP_PORT=11400

OPENWEBUI_HOST=open-webui
OPENWEBUI_PORT=8080

TTYD_HOST=ttyd
TTYD_PORT=7681

VSCODE_HOST=vscode
VSCODE_PORT=8443

SEARXNG_HOST=searxng
SEARXNG_PORT=8080

LLAMACPP_RPC_HOST=llamacpp-rpc
LLAMACPP_RPC_PORT=11401

VNC_HOST=novnc
VNC_PORT=80

LOCALAI_HOST=host.docker.internal
LOCALAI_PORT=11480

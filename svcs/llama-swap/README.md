# llama-swap

Runs llama-swap from nightly builds. llama-server is bundled in the container.

## Linux

Uses Docker to run llama-swap on the *default* Docker context (that's the one running on the Linux host).
